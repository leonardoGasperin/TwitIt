import React from 'react';
import { Text, View } from 'react-native';
import { commonStyles } from '../../../styles/CustomStyles';

export default function Postages({navigation}) {
  return (
    <View style={commonStyles.container}>
        <Text>POSTAGENS</Text>
    </View>
  );
}